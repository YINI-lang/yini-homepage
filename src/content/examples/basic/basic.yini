# A small, practical YINI config.

^ App
  name = "Demo App"
  version = "1.0.0"
  features = ["search", "logs"]
  debug = off   # Same as false
  pageSize = 25

  ^^ Server
    host = "localhost"
    port = 8080
    useTLS = false
