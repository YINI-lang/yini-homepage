---
# File: src/pages/tools/yini-parser-ts/index.mdx
title: 'YINI Parser for TypeScript/JavaScript and Node.js'
description: 'Parse YINI configuration files in TypeScript/JavaScript and Node.js with the official YINI parser for structured, human-friendly configuration.'
---

import BaseLayout from '../../../layouts/BaseLayout.astro'

<BaseLayout headTitle={frontmatter.title} headDescription={frontmatter.description}>

# Official TypeScript / JavaScript YINI Parser

**Parse YINI configuration files in TypeScript/JavaScript and Node.js.**

The official TypeScript/JavaScript parser is the primary YINI parser implementation. It is designed to make YINI easy to use in Node.js applications, scripts, tools, and services.

If you want a configuration format that stays readable for humans while still being structured and predictable for software, a YINI parser gives you a simple way to work with it in code.

---

## Install

```bash
npm install yini-parser
```

Then import it in your project:

```typescript
import YINI from 'yini-parser'
```

---

## Quick example

Suppose you have this YINI content:

```yini
^ App
name = "My App Title"
version = "1.2.3"
pageSize = 25
darkTheme = off
```

You can parse it like this:

```typescript
import YINI from 'yini-parser'

const config = YINI.parse(`
^ App
name = "My App Title"
version = "1.2.3"
pageSize = 25
darkTheme = off
`)

console.log(config.App.name)
console.log(config.App.darkTheme)
```

Output:

```js
My App Title
false
```

---

## Parse from a file

If your configuration is stored in a file, you can parse it directly:

```typescript
import YINI from 'yini-parser'

const config = YINI.parseFile('./config.yini')

console.log(config.App.version)
```

---

## What you get back

The parser returns normal JavaScript objects, so the result is easy to use in your application code.

```yini
^ App
name = 'My Title'
items = 25
darkMode = true

    ^^ Special
    primaryColor = 0x336699
    isCaching = false
```

Becomes:

```js
{
    App: {
        name: 'My Title',
        items: 25,
        darkMode: true,
        Special: {
            primaryColor: 3368601,
            isCaching: false
        }
    }
}
```

---

## Why use YINI in code?

YINI is designed to be:

- **Human-friendly** — easy to read and write.
- **Explicit** — structure is clear and not controlled by indentation.
- **Structured** — supports nested sections, lists, booleans, numbers, strings, and inline objects.
- **Predictable** — built around clear parsing rules.

---

## Common use cases

This parser is a good fit if you want:
- Application configuration loaded at runtime.
- Readable configuration files for internal tools.
- A parser for Node.js or TypeScript projects.
- Structured config without indentation-based structure.
- A format that can later also be used with the YINI CLI.

---

## Related tools

If you want to work with YINI from the command line, see the [YINI CLI page](/tools/yini-cli).

The CLI is useful for:
- Validating YINI files from the CLI/terminal.
- Converting YINI to JSON or JavaScript.
- Inspecting configuration from the terminal.

For example projects showing YINI usage in Python, JavaScript, and TypeScript see:  
[YINI demo apps ↗](https://github.com/YINI-lang/yini-demo-apps)

---

## Learn more

- ➡️ [YINI format overview](/start/what-is-yini-and-why)  
  _What YINI is and why it exists._
- ➡️ [Learn the YINI syntax](/learn-yini/learn-yini-config-format)  
  _A simple introduction to sections, values, nesting, and comments._
- ➡️ [Examples](/learn-yini/examples)  
  _See YINI examples from simple to more advanced._

---

## Links

If you want to try the parser or explore the project further, these links are a good place to continue.

- [YINI Parser on npm ↗](https://www.npmjs.com/package/yini-parser)
- [yini-parser-typescript on GitHub ↗](https://github.com/YINI-lang/yini-parser-typescript)

</BaseLayout>
