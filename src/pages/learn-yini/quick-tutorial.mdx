---
title: 'Quick Tutorial: YINI'
description: A 5-minute hands-on tutorial — write and validate your first YINI data file (text-based). It is suitable for configuration files, application settings, and general data-storage use cases.
---

import CONFIG from '../../config/conf'
import BaseLayout from '../../layouts/BaseLayout.astro'

<BaseLayout headTitle={frontmatter.title} headDescription={frontmatter.description}>

# Quick Tutorial: YINI Basics

YINI makes data-files (such as configuration files and application settings) readable for humans _and_ consistent for machines — like INI, but with **real structure**, lists (arrays), objects, and comments.

This short tutorial covers the basics of the YINI format and how to validate your configuration.

## 1) What is YINI (in one line)?

A human-friendly config format (like INI) **with real structure**: sections, clear nested sections, arrays, objects, and unambiguous, predictable rules.

---

## 2) Create your first file

Make a file named **`config3.yini`** and paste:

```yini
^ App
name = "Demo"
version = "1.0.0"
features = ["search", "dark-mode"]  // comments are allowed

^ Server
host = "0.0.0.0"
port = 8080

^^ Logging
enabled = false
```

💡 Quick syntax reference: [YINI Cheat Sheet](/refs/yini-cheat-sheet).

### 💡 What's happening

- `^` starts a section. More carets = nested section (`^^ Logging` is inside `Server`).
- `key = value` defines a setting.
- Lists (arrays): `[1, 2, 3]`, objects: `{ key: value }`.
- Comments: `//` or `#` (include a space after `#` for inline comments).
- Want more of YINI's syntax and structure? Read [Learn the YINI Format](/learn-yini/learn-yini-config-format).

---

## 3) Validate your config (in CLI)

In your terminal or command line (from the same folder as your file), run:

```bash
npx yini-cli validate ./config3.yini
```

You should see something like:

```bash
✅ Validation passed — 0 errors, 0 warnings.
```

If YINI reports 0 errors, 0 warnings, everything's fine.
A non-zero exit code means something went wrong.

### More commands and options

To explore more commands and options, run:

```bash
npx yini-cli --help
```

If you want a fuller introduction, examples, and common commands, see the [YINI CLI page](/tools/yini-cli/).

## 4) Load YINI in your app

If you want to use YINI at runtime in a TypeScript/JavaScript (Node.js) app, you can load the same file as native objects, with the [YINI Parser](/tools/yini-parser-ts).

```ts
import YINI from 'yini-parser'

const config = YINI.parseFile('./config3.yini')

console.log(config.App.name)
console.log(config.Server.host)
```

## 5) npm script

Use this in **CI** to catch config mistakes early:

```js
{
  "scripts": {
    "config:check": "npx yini-cli validate ./config3.yini"
  }
}
```

Run with:

```bash
npm run config:check
```

---

### ⚠️ Common Questions

#### Does indentation matter?

No, indentation is only for readability.  
Structure is determined by section markers (`^`, `^^`, etc.), not spaces.

#### Can I mix comment styles?

Yes, YINI supports both `//` and `#` for inline comments.  
For consistency, use just one style per file.

---

## ✅ You just wrote and validated your first YINI config!

That's the basics.

## Next Steps

- ➡️ [Get Started](/use-yini/get-started)  
  _Installation and CLI usage overview._
- ➡️ [YINI Examples](/learn-yini/examples)  
  _Shows YINI configuration examples._
- 📖 [Learn the YINI format](/learn-yini/learn-yini-config-format)  
  _10–15 minute introduction to YINI format's syntax and structure._

YINI is simple by design — experiment, explore, and see what you can build.

</BaseLayout>
